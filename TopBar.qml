import QtQuick

Rectangle {
    id: topbar
    height: Math.max(parent.height / 10, 40)
    anchors{
        top: parent.top
        left: parent.left
        right: parent.right
    }
    color: "#303030"

    /* TODO: menu bar & tab selection go here */




}
